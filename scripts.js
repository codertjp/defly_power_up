// console.log(chrome);

// function send(message) {
//     chrome.runtime.sendMessage(message, (response) => {});
// }

// chrome.runtime.onMessage.addListener(function (request, sender, sendResponse) {
//     //   Find a run code depending on sent message
//     if (request.message.match("log") !== null) {
//         document.getElementById('logs').innerHTML = document.getElementById('logs').innerHTML + message.replace("log", "");
//     }
//     // Acknowledge message
//     sendResponse("ok");
//   });
//   send('JSON');


//   chrome.devtools.panels.elements.createSidebarPanel("My page");
